services:
  app:
    build: .
    ports: ["8080:8080"]
    environment:
      DATABASE_URL: postgresql://service:localdev@db:5432/service
    depends_on: [db]
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: service
      POSTGRES_USER: service
      POSTGRES_PASSWORD: localdev
